extends layout

block content
    p Welcome to #{title}
    p Hér á að koma dagbók með userum og eh svoleiðis
    div.publicEntries
        each entry in result
            div.entry
                h3 #{entry.title}
                h4 #{entry.date}
                p #{entry.text}    